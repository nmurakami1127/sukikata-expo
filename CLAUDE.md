# CLAUDE.md

このファイルはClaude Codeがこのリポジトリで作業する際の起点となる指示書です。作業開始前に必ず読み、関連する`docs/`配下のドキュメントも参照してください。

## プロジェクト概要

**スキマかたづけ（愛称：スキかた）**
「片付けよう」を「片付いた」に変える、空き時間の5分から始める片付け習慣アプリ。

- プラットフォーム：iOS / Android（React Native または Flutter、1ソースで両対応）
- 公開状況：MVP公開済み。現在は「タスクパーソナライズ／ロボット掃除機対応」を次期リリースとして開発中
- オフラインファースト：タスク表示・タイマー・写真保存・ストリークは端末内処理。計測イベントのみオンライン送信

## ドキュメント構成

作業内容に応じて、以下を必ず参照すること。

| 目的 | パス |
|---|---|
| 公開済みMVPの仕様（企画確定版） | `docs/requirements/mvp-spec.md` |
| 次期リリースの要求定義（企画確定版） | `docs/requirements/robot-vacuum-personalization.md` |
| 次期リリースの実装指示（エンジニア向け変換版） | `docs/implementation/robot-vacuum-personalization.md` |

`docs/requirements/`配下は企画側が確定した「何を作るか」。`docs/implementation/`配下はそれを実装タスクに変換した「どう作るか」。両者が矛盾する場合は`docs/requirements/`を正とし、作業を止めて確認すること。

## 絶対に守る基本方針（全機能共通）

新機能の追加・既存機能の変更にかかわらず、以下は常に維持する。プロダクトの前提であり、個別のIssueやPRで覆してはならない。

- AIを利用しない（画像解析・推論・生成AIロジックを一切組み込まない）
- アカウント登録・ログインを求めない
- 広告・課金導線を設けない
- 個人情報・位置情報を取得しない
- 広告SDK・外部トラッキングSDKを導入しない
- 基本機能は端末内で完結させる（計測イベントの送信のみオンライン）
- 選択疲れを増やさない（新しい判断・入力・モード選択をユーザーに安易に追加しない）
- 完璧を求めない（「全部」「毎日」を要求するタスク文言・機能を作らない）

これらに抵触しそうな実装が必要になった場合は、実装を進めず一度立ち止まって確認すること。

## コーディング前のチェックリスト

1. 該当機能の要求定義（`docs/requirements/`）を読み、企画意図を確認する
2. 実装指示（`docs/implementation/`）のスコープ外セクションを確認し、対象外の機能に触れていないか確認する
3. 既存のタスク選択ロジック・ストリーク・通知など既存機能への回帰がないか、変更範囲を最小限にする
4. タスクデータ（JSON）を変更する場合は後方互換性を保つ（既存タスクが新フィールドなしでも動作すること）

## 現在の開発フォーカス：ロボット掃除機対応

`docs/implementation/robot-vacuum-personalization.md`の「6. 実装順序の提案」に沿って進める。特に以下は誤解しやすいので実装前に該当箇所を読み直すこと。

- ホーム画面バナーの再表示ロジック（単純なタイマー式にしない。「床カテゴリの再利用」が再表示条件）
- 「今後出さない」は候補枯渇時にもアプリ側から自動解除しない
- ロボット掃除機向けタスクは既存タスクデータと共用し、専用に複製したタスクリストを作らない
- タスク表示直前〜褒めメッセージ表示中は、新しい案内やアンケートを一切割り込ませない

未確定の定数（例：床カテゴリ利用回数の閾値）は実装指示書内の「要確認事項」を参照し、暫定値を定数化した上で進める。

## テスト方針

- 既存MVPの回帰確認を優先する（`robotVacuumStatus`未設定ユーザーの挙動が変わらないこと）
- 属性フィルタ・非表示設定・フォールバックロジックはユニットテストで分岐を網羅する
- UIの表示タイミング制御（バナーが出てはいけないタイミング）は自動テストで確認する
